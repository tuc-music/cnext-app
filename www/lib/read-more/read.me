readmore.js has been modified by Nektarios as follows:

line 117: if(vm.hmLinkify) vm.lessText = $filter('linky')(vm.lessText);
line 121: if(vm.hmLinkify) vm.moreText = $filter('linky')(vm.moreText);

these two lines allow to linkify text exploiting 'linky' filter of ionic.
